{
  "name": "web_neuma_2022",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "gh-pages -d dist",
    "purgecss": "purgecss --css css/main.css --content dist/index.html --output dist/style.css",
    "sass": "sass --watch sass:css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.1",
    "bootstrap-icons": "^1.9.1",
    "gh-pages": "^4.0.0",
    "sass": "^1.55.0"
  }
}
